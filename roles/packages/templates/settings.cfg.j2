[CONFIG]
enable_debug = False

# Plex server settings
plex_url = {{ plex_url }}
plex_token = {{ plex_token }}

# Metadata settings
prefer_local_art = True
poster_item_limit = 5
background_item_limit = 2

# TMDB settings
api_key = 59bb0db203a09d2820a27734437c3bd6  # This belongs to some random fella on a forum and I don't want to sign up, until it stops working just using it